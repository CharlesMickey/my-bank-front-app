apiVersion: v1
kind: Service
metadata:
  name: transfer-service
spec:
  type: {{ .Values.service.type }}
  selector:
    app: transfer-service
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
