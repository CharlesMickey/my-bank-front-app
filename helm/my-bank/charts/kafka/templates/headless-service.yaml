apiVersion: v1
kind: Service
metadata:
  name: kafka-headless
spec:
  clusterIP: None
  selector:
    app: kafka
  ports:
    - name: kafka
      port: {{ .Values.service.clientPort }}
      targetPort: kafka
    - name: controller
      port: {{ .Values.service.controllerPort }}
      targetPort: controller
