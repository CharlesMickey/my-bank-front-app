apiVersion: v1
kind: Service
metadata:
  name: accounts-service
spec:
  type: {{ .Values.service.type }}
  selector:
    app: accounts-service
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
