apiVersion: v1
kind: Service
metadata:
  name: elasticsearch
spec:
  type: {{ .Values.service.type }}
  selector:
    app: elasticsearch
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
