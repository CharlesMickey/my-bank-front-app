replicaCount: 1

image:
  repository: docker.elastic.co/elasticsearch/elasticsearch
  tag: "8.15.2"
  pullPolicy: IfNotPresent

service:
  type: ClusterIP
  port: 9200

resources:
  requests:
    cpu: "250m"
    memory: "768Mi"
  limits:
    cpu: "1"
    memory: "1Gi"
