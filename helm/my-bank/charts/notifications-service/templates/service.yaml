apiVersion: v1
kind: Service
metadata:
  name: notifications-service
spec:
  type: {{ .Values.service.type }}
  selector:
    app: notifications-service
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
